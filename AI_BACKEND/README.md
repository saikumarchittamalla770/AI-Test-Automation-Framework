# AI E2E Playwright — Complete Project

This project generates Playwright tests using GPT-4.0-mini, runs them, and stores artifacts.
Run `npm install` then `npx playwright install` before using.

Scripts:
- `npm run cli` : Run CLI to analyze repo and run tests.
- `npm start` : Start server + UI to browse artifacts.

